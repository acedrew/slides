---
intro:
    background: '{{ site.navy-background }}'
sectionbreak:
    background: '{{ site.navy-background }}'
list:
    background: '{{ site.navy-background }}'
contact:
    background: '{{ site.navy-background }}'
image:
    background: '{{ "/assets/img/" | relative_url }}{{ slide.image }}'
    background-transition: "{{ slide.background-transition }}"
